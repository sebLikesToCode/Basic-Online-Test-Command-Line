# Basic-Online-Test-Command-Line
a local internet command line mvement script with names
# Bugs and challenges
the bigest challenge was learning the socket syntax, and how socket worked. it was confusing but ive got it down pretty good. the other hard thing was rendering the neames, i had to redo my print_board function to facto them in, then had to make sure they didnt clip through the walls.
# notes
if you want to run it, run the server first, than open the client program on as many windows as you want. if you want other devices on the same internet to connect, change the server.listen from localhost to 0.0.0.0
